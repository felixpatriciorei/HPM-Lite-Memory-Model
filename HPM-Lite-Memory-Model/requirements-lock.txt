# Reproducibility note
#
# This project intentionally keeps requirements.txt as the install source for CI and normal use.
# Do not generate this file with pip freeze from a global developer machine.
# A true environment lock should be generated from a clean virtual environment only.
#
# Canonical install:
#   python -m pip install -r requirements.txt
#
# Test command:
#   python -m pytest -q
