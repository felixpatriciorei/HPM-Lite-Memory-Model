# Legacy figure entry points

These scripts are archived compatibility entry points from earlier experiment snapshots.

Use the canonical figure command from the repository root:

```bash
python scripts/make_figures.py